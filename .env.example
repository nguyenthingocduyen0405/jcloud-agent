# Local development only. Never put passwords, tokens, or cloud credentials here.
DATABASE_PATH=data/jcloud_agent.db
API_HOST=127.0.0.1
API_PORT=8000
VITE_API_URL=http://127.0.0.1:8000

# LLM parsing layer. Mock mode is local and needs no API key.
LLM_PROVIDER=mock
LLM_MODEL=
LLM_API_KEY=
